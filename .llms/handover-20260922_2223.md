# Handover — 2026-09-22 22:23

Session spanned five work streams; **everything is committed** (tree is clean, latest
`d7b1520`). No uncommitted work, no blockers. Repo rules live in `AGENTS.md`; design
rationale per package in `.llms/architecture.md` — read the section for whatever package
you touch next.

## State of the repo

- Branch state: clean working tree, 8 commits this session, none pushed (repo rule: never
  push unless asked).
- Go 1.27.1 toolchain; all validations green at handoff: `task lint`, `task check`,
  `go test ./...` (22 packages), `go test -tags debug ./cmd/ ./database/`, `gofmt`.

## What was done (by stream, newest last)

1. **Storage engine hardening + upload hooks** (commits before this session's history —
   see `.llms/architecture.md` `internal/storage` and the memory ids below). Key contract:
   `Manager.Stage` → queue job → `Sync` (before-hook → fingerprint → checkpoint/fast-path
   → HasChunks diff → parallel upload → ready commit → after-hook → clearStaging).
   Hooks are nil by default; `parallelHashThreshold` is a test-overridable var.
2. **Package READMEs + slim root README** (commits `9187367`, `b944814`).
3. **sqlbuilder rule codified** (`78d74fa`): every query against application tables goes
   through `huandu/go-sqlbuilder` (PostgreSQL flavor, see `internal/queue/store.go` for
   the idiom); raw SQL only in tests and `database/` maintenance tooling. Documented in
   `AGENTS.md` Conventions + `.llms/architecture.md` `internal/registry`.
4. **encoding/json/v2 rule codified** (`258da33`, `200aac3`): production code imports
   `encoding/json/v2` only; `encoding/json` is test-only. `AGENTS.md` links the
   migration guide and lists the behavior deltas to check (nil map → `{}`, dup keys,
   invalid UTF-8).
5. **Go ≥1.25 modernization audit** (`d3de0a2`): ran `go fix -diff` (the 1.26/1.27
   modernizers) and applied all suggestions — `errors.AsType`, `maps.Copy`,
   `strings.SplitSeq`, `for range n`, `sync.WaitGroup.Go`, LogLayer `BaseConfig`→`ID`
   via `//go:fix inline`. Also fixed pre-existing breakage: migration-count assertions
   still said 8 after migration 00009 landed (the AGENTS.md gotcha), plus 5 errcheck
   findings in tests. Verdict summary: repo already idiomatic (b.Loop, for range n,
   omitzero, context.WithoutCancel, no math/rand v1 / sort / SetFinalizer); runtime
   freebies on 1.27 (Green Tea GC, size-specialized malloc, container-aware GOMAXPROCS
   — nothing in the repo overrides it); skipped with reasons: `os.Root` staging
   hardening (ValidateKey suffices), `testing/synctest` for the watcher (fsnotify events
   are not bubble-isolatable), `trace.FlightRecorder`, `net/http.CrossOriginProtection`.
6. **Migration tests de-strictened** (`cc7724f`): no more hardcoded counts. Added
   `database.EmbeddedMigrations() ([]EmbeddedMigration{Name,Version,SQL}, error)` in
   `database/validate.go`. Tests derive counts/versions/names from it:
   `database/migrator_test.go` (shared helpers `migrationCount()`/`highestVersion()` via
   `sync.OnceValue`, reused by `progress_test.go`/`validate_test.go`),
   `cmd/migration_test.go` (`embeddedFiles`/`migrationTotal()`/`latestMigration()`/
   `migrationTables()` — helpers live in the untagged file because
   `migrate_debug_test.go` and `migrate_seed_test.go` are `//go:build debug`),
   and `database/backup_test.go` now compares the restored table SET against the source
   (filtering `database.VersionTable` — the dump deliberately excludes `app_migration`).
   "Table must be gone" assertions derive created tables by regexing the migration SQL
   (`CREATE TABLE [IF NOT EXISTS] name`). Deliberately still pinned content assertions:
   00001 creates the `reference` schema; `rate_limits` survives newer rollbacks.
   AGENTS.md gotcha updated: adding a migration file needs zero test edits.
7. **task lint fix** (`d7b1520`): 15 findings from the repo config
   (`.config/golangci.yml`: govet `enable-all` incl. `shadow`, plus gosec). 14 shadow
   findings fixed at source in `internal/storage/{manager,manifest,manager_test}.go`
   (de-shadow `err`, extracted `Manager.saveManifest` helper); 1 gosec G602 in
   `internal/cache/cache_valkey.go` fixed with an `if i < len(keys)` guard wrapping the
   index (the only guard shape G602 accepts).

## Gotchas the next agent must know

- **Lint with `task lint` (or `golangci-lint run -c .config/golangci.yml ./...`), never
  bare `golangci-lint run`** — the config enables govet `shadow` and gosec; bare runs
  hide them and gave this session a false "0 issues".
- govet `shadow` flags any inner `err :=` when an outer `err` exists and is used later;
  fix by `err = ...` or renaming. gosec G602 only accepts an `if i < len(s)` guard
  lexically wrapping the index.
- `cmd/migrate_debug_test.go` and `cmd/migrate_seed_test.go` are `//go:build debug` —
  run `go test -tags debug ./cmd/` to compile them; shared test helpers must live in an
  untagged `_test.go` file in the same package.
- The OTel `otel/log` pin (v0.19.0, three modules) is fragile after any `go get` —
  re-check and re-pin per AGENTS.md.
- Never commit/push on own initiative; never `git add -A`; recommend a commit title and
  let the user commit.

## Open threads / suggested next steps

- `Manager.Progress` is still a stub — `TODO(notification)` in `.llms/architecture.md`
  covers progress serving; `AfterSyncHook` is the intended push path for "upload
  finished" events.
- `internal/kernel`, `internal/transport`, `internal/registry` remain scaffolds (see
  `.llms/architecture.md`); ConnectRPC routes and auth middleware still wait for pieces.
- Optional hardening backlog (explicitly deferred, do not do without asking): `os.Root`
  for staging, `testing/synctest` for watcher debounce, `net/http.CrossOriginProtection`
  when browser-facing endpoints arrive.

## Suggested skills

- **golang-samber-do** — before touching `internal/registry`/DI wiring.
- **finalize** — if the next session ends with a commit-worthy change.
- **go-api-perf** / **go-http-safeguards** — when building the ConnectRPC routes and
  middleware in `internal/transport`.
- **impeccable** / **show-me** — if the next session is review- or demo-oriented.

## Pointers instead of duplication

- Architecture decisions and per-package invariants: `.llms/architecture.md`
  ("Library decision records" holds the settled cache/kvstore/scheduler/LogLayer/OTel
  comparisons — do not re-run them).
- Codified rules: `AGENTS.md` (Conventions: sqlbuilder rule, json/v2 rule, config
  precedence, migration gotcha as updated).
- Package docs: `README.md` files under `internal/{config,datastore,cache,logger,
  observer,health,storage,queue,scheduler}` and `database/`.
- Prior handovers: `.llms/handover-20260922_1941.md` (storage hardening details,
  test-infra facts: testcontainers Postgres/MinIO, chunk size 32 bytes in tests,
  salted S3 fixtures, watcher 50 ms debounce).

No secrets or PII appear in this session's work; nothing to redact beyond what is
already above.
