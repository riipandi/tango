# Handover — 2026-09-23 17:49

## Next session

Continue developing feature modules. The immediate candidate is the **second area**: the
`Area` seam has exactly one real implementation (`identity`), and a second area is where the
pattern is actually proven. `apikey` is the smallest of the six stubs.

Before the first module that verifies a real token, settle the **verifier algorithm pinning**
note below — it is the one place where "tests pass" does not equal "safe in production".

## State

- Branch `refine-architecture`, HEAD `79952a3`, working tree clean, nothing stashed.
- Six audit defects fixed and committed, one per commit: `1753420` (datastore Shutdown),
  `5f0d7a3` (queue drain), `4e10744` (limiter scope), `0978aa2` (healthz target),
  `dfb0af6` (release window), `3fe2899` (static mount). Rationale per fix lives in
  `.llms/architecture.md`; commit messages carry the why. Do not restate either here.
- Follow-up work after the six: `5a5d84a` moved the uploads mount to `internal/transport/static`,
  `79952a3` put a driver seam behind it.
- Validation last run green: `gofmt`, `go vet`, `golangci-lint` (0 issues), `go test ./...`,
  and `-race` over `internal/queue`, `internal/transport/...`, `internal/health`,
  `internal/datastore`.

## Open notes carried forward

### 1. Verifier does not pin `alg` (decide before a module verifies a token)

`pkg/jwtutils/verifier.go` (~line 87) sets `jws.WithRequireKid(true)` but pins no algorithm:

```go
opts = append(opts, jwt.WithKeySet(v.keySet, jws.WithRequireKid(true)))
```

jwx v3.3.0 has no `WithAllowedAlgorithms`. A token header naming an algorithm the published
set does not carry is therefore not rejected on that basis alone. The intended shape is a
header check in a verifier wrapper, with the allowlist derived from the published keys —
not a hand-kept list that can drift from `auth.jwt_algorithm`.

Related, and the reason this is not urgent yet: the sign/verify path has **zero production
callers**. `jwtutils.NewSigner`, `jwtutils.NewVerifier`, `SigningAlgorithm`, and
`HMACKey()` are exercised only by tests. The publish path (JWKS) is end-to-end verified; the
issue path is not.

### 2. The `Area` pattern has one real implementation

```
modules/identity/module.go    119 lines  real
modules/{apikey,appconfig,auditlog,devicelogin,federation,webhook}/module.go  1 line  stub
```

The seam is proven by one area plus a synthetic test (`internal/registry/compose_test.go`).
Build the second area before relying on the pattern further. Inside `identity`, only `jwks`
has tests; the other twelve features are 4–5 file scaffolds.

### 3. Storage upload progress endpoint is still a stub

`internal/storage/manager.go` (`Progress`, `TODO(notification)`), documented in
`.llms/architecture.md`. The data is live (`chunks_done`/`chunk_count`); nothing serves it.
Documented, not a defect — do not treat it as broken, and do not build a second progress
source beside the row.

## Environment notes

- `app.config.json` is gitignored and was regenerated this session
  (`go run -tags debug ./cmd config:generate --overwrite`); `queue.release_after` is now
  `3600`. A deployment carrying a stale value fails the run before the listener with a
  clear message — that is intended, not a bug to work around.
- `.env.local` outranks the shell environment for config values; flip a setting with
  `--config-file`, not an exported variable.
- The agent memory store is **read-broken** right now: writes fail with
  `FTS index idx_memories_fts was created by an older version of Turso`. Fix is
  `DROP INDEX idx_memories_fts` then recreate it `USING fts`. Until then, this document and
  the git history are the durable record.

## Suggested skills

- `golang-samber-do` — the container, the `Area` seam, and lifecycle shutdown all touch it.
- `handoff` — for the next handover.
- `go-http-safeguards` — relevant when a module adds routes or a proxy sits in front (the
  limiter keys on the client address today; trusted-proxy handling is an open decision).
- `finalize` — only if this branch is merged back.
