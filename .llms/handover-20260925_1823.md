# Tango Session Handoff — 2026-09-25 18:23 (UTC+7)

Handoff for a fresh agent continuing work on `tango` (riipandi/tango), the modular-monolith Go
auth server. The working tree is **clean**; every piece of work below is committed.

## What this session did (chronological, committed)

1. **Identity sign-in feature** (earlier commits, fully documented in code and
   `.llms/architecture.md` → "modules/identity/signin"):
   - `tango.auth.v1.AuthService/SignIn` over ConnectRPC: single `identity` field (CITEXT
     username, case-insensitive OR exact email), JWT access token via `jwks.Service` (dual
     stack), refresh token stored as SHA-256 hash in `public.sessions` (`sess_` typeid, UUID in
     DB column), anti-enumeration dummy-hash path, disabled/banned → `permission_denied`.
   - Remember-me TTLs: `auth.refresh_short_ttl` (12h) / `auth.refresh_long_ttl` (14d); the old
     `auth.refresh_ttl` and the whole `session.*` config section were **removed** (no compat
     shims — project rule). `auth.session_driver` (SessionDB|SessionKV) replaced `session.driver`.
2. **Yaak workspace reorganization** — 93 requests aligned to Pocket ID's 15 sections plus
   `Authentication`, prefixes `[Pocket ID]`/`[Tango]`, `(unimplemented)` suffixes. See
   `AGENTS.md` → Gotchas for the conventions and the CLI-only rule.
3. **RPC wiring fix** — commit `f4950be`: an area module must implement `kernel.RPCModule` or
   `kernel.MountRPC` silently skips it and every procedure answers "unknown procedure".
   `identity.Module` now forwards `MountRPC` to its features; pinned by
   `TestTheAreaForwardsFeatureProcedures`.
4. **`expires_in` wire-type fix** — commit `9b9e92a`: protobuf's JSON mapping serializes int64
   as a JSON **string**; the field is now `int32` in `api/connect/auth.proto` (field number 3
   unchanged) so it serializes as a number. Pinned by
   `TestTheCodecWritesALifetimeAsANumber` in `internal/transport/handler_rpc_test.go`.
   **Rule of thumb going forward: any proto field a client reads as a JSON number must be
   `int32` in `api/connect/*.proto`.**
5. **Structure review of `modules/`** — verdict recorded: solid. Implemented today in identity:
   `jwks`, `signin` (full features) and `session`, `password`, `user` (schema-holder packages,
   not features). All other packages/areas are 1-line inert scaffolds. Promotion rule for
   shared types: wait for a second consumer, then promote (e.g. `signin.AccessClaims` →
   `pkg/jwtutils` once refresh/sign-out needs to verify tokens).

## Operational notes

- End-to-end verification recipe that worked: `docker compose -f docker/compose.yaml up -d
  pgsql`, `task db:migrate`, `task run -- serve --host localhost --port 3080` (background),
  curl `POST /rpc/tango.auth.v1.AuthService/SignIn`. Kill with `lsof -ti tcp:3080 | xargs kill`
  — killing the `task run` wrapper pid does **not** kill the child holding the port.
- `edit_file` intermittently left residue (duplicated blocks / stray fragments) in
  `modules/identity/module.go` and `signin/service.go`. Always re-read or grep after an edit;
  prefer `write_file` full rewrite for heavily edited files.
- OrbStack (docker daemon) may need `open -a OrbStack` + ~30s wait before testcontainers work.
- Validation state at handoff: `task test:go` 926 tests pass, `task lint` and `task check`
  clean, sign-in verified live (200 with token pair; wrong password → 401 `unauthenticated`).

## Open / next work (user-hinted, in rough priority)

1. **SignOut + refresh token rotation** — the natural next procedure on `AuthService`; the
   sessions table and hashed-token pattern are already in place. When a second feature needs to
   verify access tokens, promote `signin.AccessClaims` and the claims-building helper to
   `pkg/jwtutils`.
2. **MFA flow** (`modules/identity/multifactor` scaffold exists; contract deferred by design).
3. **Per-endpoint rate budget.**
4. **Yaak mirror sync**: verify `api/specs/` reflects the `(unimplemented)` renames once the
   Yaak app flushes file-sync; never hand-edit those YAMLs — use the `yaak` CLI
   (workspace `wk_kBiMYTkhPP`).

## Reference artifacts (do not duplicate, read instead)

- `AGENTS.md` — project rules; Gotchas → `api/specs/` conventions; Common Tasks → "add an
  endpoint / feature module" recipes.
- `.llms/architecture.md` — per-package rationale; sections "modules/identity/signin" and
  "Library decision records".
- `.llms/endpoint-reference.md`, `.llms/pocket-id-api-reference.md`, `.llms/tango-deviations.md`
  — API surface and upstream comparison.
- Commits `f6a76fc`, `f9f48c5`, `f4950be`, `9b9e92a` — this session's tail; earlier commits
  cover the sign-in feature and config refactor.

## Suggested skills

- `golang-samber-do` — before touching any DI wiring (`internal/registry`, area `Package` /
  `Deps`); the project's DI conventions live there.
- `handoff` — when this next session ends, to produce the following handover document.
