# Handover — 2026-09-23 19:13

## Next session

Continue developing feature modules. Nothing is half-finished and the tree is clean, so the
next session can pick any of the three items under "Open work" without first repairing
anything.

The highest-value next move is still the one the previous handover named: build the **second
area** (`apikey` is the smallest of the six stubs). The `Area` seam has exactly one real
implementation, and a second one is where the pattern is actually proven.

## State

- Branch `refine-architecture`, working tree clean, nothing stashed.
- HEAD `1d93745` — the four audit defects (D1–D4) fixed in one commit. `bace854` is the
  first half of D1 (the settle context); `1d93745` carries D2, D3, D4 and the `stop()` fix.
- `51ecba2` added `coverage` / `coverage:report` to `tasks/test.yml` and excluded `codegen`
  from the coverage set. Not written by the agent; it was already in the tree.
- Validation last run green: `gofmt`, `go vet`, `golangci-lint` (0 issues), `go test ./...`,
  `go test -tags debug ./cmd/ ./database/...` (run twice), and `-race` over `internal/queue`.
- Runtime verified: `health=200`, SIGTERM and SIGINT both clean, a real `db:export` dump is
  `-rw-------` including the `--overwrite` path.

Do not restate the fixes from the commits — the messages and the diffs carry the why, and
`.llms/architecture.md` carries the per-package rationale. The regression tests are named in
the commit bodies; all seven are present.

## What the audit covered

The audit request was `cmd/`, `database/`, `internal/`, `modules/identity/jwks/` — race
conditions, leaks, defects, vulnerabilities. Four defects were found and are now fixed.
These areas were checked and came back clean, so a later session need not re-walk them:

- **SQL injection** — `database/` consistently uses `pgx.Identifier{}.Sanitize()` for
  identifiers and `$n` for values; `Table.Qualified()` sanitises. Raw SQL is confined to
  `database/` maintenance tooling, as `AGENTS.md` requires.
- **Goroutine lifetime** — health check results channel is buffered `len(due)`, the done
  channel is buffered 1, `runCheckFunc` recovers panics. No leak path found.
- **`internal/cache/cache_memory.go`** — per-shard `RWMutex`, collision slots store the key
  and verify bytes, monotonic clock, `nextChunk` clears both `m` and `collisions`. No
  corruption path.
- **`internal/logger/async.go`** — `Close()` idempotent via `sync.Once`, bounded drain,
  `closeSink` bounded by context.
- **`internal/storage/watcher.go`** — timers touched only from the loop goroutine; the
  "no lock" claim in its comment holds.
- **`modules/identity/jwks/repository.go`** — the private key column is never selected;
  `rows.Close()` and `rows.Err()` are both handled.

## Open work

### 1. Verifier does not pin `alg` (decide before a module verifies a token)

Unchanged from the previous handover. `pkg/jwtutils/verifier.go` (~line 87) sets
`jws.WithRequireKid(true)` but pins no algorithm, and jwx v3.3.0 has no
`WithAllowedAlgorithms`. The intended shape is a header check in a verifier wrapper, with
the allowlist derived from the published keys.

Still not urgent, and for the same reason: the sign/verify path has **zero production
callers**. `jwtutils.NewSigner`, `jwtutils.NewVerifier`, `SigningAlgorithm`, and `HMACKey()`
are exercised only by tests. The publish path (JWKS) is end-to-end verified; the issue path
is not.

### 2. The `Area` pattern has one real implementation

Unchanged. `modules/identity/module.go` is real (119 lines); `apikey`, `appconfig`,
`auditlog`, `devicelogin`, `federation`, and `webhook` are one-line stubs. Inside `identity`,
only `jwks` has tests — the other twelve features are 4–5 file scaffolds.

### 3. Storage upload progress endpoint is still a stub

Unchanged. `internal/storage/manager.go` (`Progress`, `TODO(notification)`). The data is live
(`chunks_done`/`chunk_count`); nothing serves it. Documented, not a defect — do not treat it
as broken, and do not build a second progress source beside the row.

## Notes for the next session

- **The agent memory store is still read/write broken**: writes fail with
  `FTS index idx_memories_fts was created by an older version of Turso`. Fix is
  `DROP INDEX idx_memories_fts` then recreate it `USING fts`. Until then, this document and
  the git history are the durable record — do not rely on `memory_search`.
- **Handover files live in `.llms/`, not `llms/`.** The handoff skill says `CWD/llms/`, but
  the repo keeps them in `.llms/` (alongside `architecture.md`) and `AGENTS.md` forbids
  adding a top-level directory. Follow `.llms/`.
- **`app.config.json` is gitignored** and was regenerated this session
  (`go run -tags debug ./cmd config:generate --overwrite`); `queue.release_after` is `3600`.
  A deployment carrying a stale value fails the run before the listener opens — intended.
- **`.env.local` outranks the shell environment** for config values. Flip a setting with
  `--config-file`, not an exported variable.
- **`cmd` tests are behind the `debug` build tag.** `go test ./cmd/` reports "no tests to
  run"; use `go test -tags debug ./cmd/`.
- **One known flake.** `TestDBExportImportCompressionRoundTrip` (`cmd`) failed once in a
  parallel full-suite run, then passed on its own and in two subsequent full runs. It is
  testcontainers contention, not a code defect — it does not touch file permissions. Do not
  widen a timeout to chase it; re-run and confirm.
- **Two of the agent's own tests were initially non-discriminating.** The lesson is worth
  keeping: a regression test that still passes after the fix is reverted proves nothing.
  Each of the seven new tests was verified to fail when its fix was reverted, and the two
  that did not were rewritten until they did.

## Suggested skills

- `golang-samber-do` — the container, the `Area` seam, and lifecycle shutdown all touch it.
- `handoff` — for the next handover.
- `go-http-safeguards` — relevant when a module adds routes, or when a proxy sits in front
  (the rate limiter keys on the client address today; trusted-proxy handling is an open
  decision, not a bug).
- `finalize` — only if this branch is merged back.
