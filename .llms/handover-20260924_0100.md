# Handover — 2026-09-24 01:00 (updated with the OTEL/Perses session)

## Next session

Continue feature-module work. Nothing is half-finished; the tree carries two
finished, validated work pieces awaiting the user's commit (the agent does not
commit without an explicit instruction in the same turn):

1. `refactor(api): centralize serve lifecycle, detect route conflicts, trim
   RPC metadata to pagination`
2. `fix(transport): refuse limited requests in the caller's protocol and spare
   both health endpoints` — plus the OTEL/Perses integration from the later
   part of the session (see "What this session settled", item 5).

## State

- Branch `refine-architecture`. HEAD unchanged from the previous handover
  (`1d93745`); this session's work is uncommitted in the working tree.
- Modified: `.llms/architecture.md`, `api/connect/common.proto`,
  `api/connect/system.proto`, `cmd/serve.go`, `docs/api-response.md`,
  `internal/kernel/kernel.go`, `internal/registry/registry.go`,
  `internal/observer/{metrics.go,observer_test.go,README.md}`,
  `internal/transport/middleware/ratelimit{,_test}.go`,
  `internal/transport/{handler_rpc,handler_rpc_test,router_http,router_rpc,server}.go`,
  `internal/transport/ratelimit_scope_test.go`,
  `docker/compose-metrics.yaml`.
- Deleted: `docker/perses/provision.sh` (dead legacy; native provisioning
  needs no script).
- New: `internal/kernel/kernel_test.go`,
  `internal/registry/lifecycle_test.go`,
  `docker/perses/projects/tango.yaml`,
  `docker/perses/dashboards/{app-logs,application,go-runtime-metrics,postgres,redis,traces}-dashboard-cr.yaml`.
- `.gitignore` is also modified — **not by this session**; check with the user
  before staging it.
- Validation green: `go build ./...`, `go vet ./...`, `task lint` (0 issues),
  `gofmt`, `task rpc:stale`, `task typecheck`, `go test ./...` (all), and
  `go test -tags debug ./cmd/ ./database/...`.
- Runtime verified against the compose stack: `task metrics:up` green; all six
  dashboards provisioned into the `tango` project (verified over the Perses
  API, stale ones deleted); `serve` with `OTEL_METRICS_ENABLE=true
  OTEL_TRACING_ENABLE=true LOG_TRANSPORT=console,file,otlp` ships all three
  signals — `go_*`/`process_*`/`http_server_*`/`rpc_server_*` series in
  VictoriaMetrics, `tango` logs in VictoriaLogs (`service.name` field), `tango`
  traces in VictoriaTraces; the Perses MCP server answers MCP initialize on
  `:3880/mcp`. SIGTERM drains clean.

## What this session settled

Five threads, all closed. The reasoning lives in the artifacts; do not restate
it here.

1. **DI/lifecycle analysis → implemented.** Warm-up and runner ordering moved
   from `cmd/serve.go` into `internal/registry` (`Prewarm`, `Runner`,
   `Runners`); route-conflict detection added to `kernel.Mount`/`MountRPC`.
   Rationale in `.llms/architecture.md` → "internal/registry" and
   "internal/kernel".
2. **Response contract → implemented.** `ResponseMetadata` trimmed to the
   pagination-only `ListMetadata`; `CheckResponse` carries no metadata;
   correlation id is written once by the request middleware. A duplicate
   `X-Request-Id` in RPC responses (an interceptor and, before it, an error
   `Meta().Set` writing the header a second time; connect merges handler-set
   headers by appending) was found and fixed — the single-writer rule is
   pinned by tests. `docs/api-response.md` rewritten as the per-transport
   mapping (one contract, each idiom).
3. **Rate limiter made consistent across transports → implemented.** The
   throttled surface is two chi groups, each refusing in its caller's
   protocol (`restRefuse` envelope / `rpcRefuse` connect
   `resource_exhausted`); exclusion lists are per transport
   (`httpRateLimitExclusions` / `rpcRateLimitExclusions`), and both health
   endpoints are excluded — the RPC readiness used to be throttled while its
   REST twin was not. Rationale in `.llms/architecture.md` →
   "internal/transport".
4. **Protocol and auth decisions → recorded.** ConnectRPC is the primary
   protocol (internal/backoffice); REST serves external integrations and the
   planned OAuth2/OIDC surface; authentication is stateless (Bearer JWT +
   `X-API-Key`, no auth sessions). One correction was made mid-session: the
   JWT signing stack is a **config decision** (`auth.jwt_algorithm`, derived
   from the material when unset), not an audience decision — the audience
   only constrains verifiability. All of it is recorded in
   `.llms/architecture.md` → "Protocol and authentication (settled
   2026-09-24)" and mirrored in agent memory.
5. **OTEL/LGTM + Perses → implemented.** The Prometheus bridge now registers
   the Go runtime and process collectors deliberately (`internal/observer`,
   `newRegistry`); the transport is instrumented (`otelconnect` interceptor in
   `rpcHandlerOptions`, `otelhttp` wrapper in `NewServer`); the Perses MCP
   server is enabled in compose; the four Grafana-migrated dashboards
   (placeholder queries) were replaced with five working ones plus an
   application-monitoring dashboard. The provisioning traps and the verified
   plugin kinds are recorded in `.llms/architecture.md` → "observability
   stack (docker)" and in agent memory.

## Open work

1. **Verifier does not pin `alg`** — now a **prerequisite** for the first
   protected procedure, no longer a note. See the jwks section of
   `.llms/architecture.md` for the intended shape (header check in a
   verifier wrapper; jwx v3.3.0 has no `WithAllowedAlgorithms`).
2. **`connectrpc/authn-go` interceptor** — the planned seam for Bearer auth
   on the Connect side, beside `rpcHandlerOptions`; REST twin is middleware
   inside the throttled group. Gated on (1). API-key rows are stored hashed
   per the token-row convention.
3. **Second area (`apikey`)** — still the highest-value move; the `Area`
   seam has one real implementation. It is also the first consumer of the
   trimmed `ListMetadata` and the auth seam.
4. **Storage upload progress endpoint** — documented stub, not a defect.

## Notes for the next session

- **Stack port map (host side)**: Perses `3380`, Perses MCP `3880`,
  VictoriaLogs `33801`, VictoriaMetrics `33802`, VictoriaTraces `33803`; the
  OTLP collector keeps `4317`/`4318`. Container-internal traffic uses the
  backends' own ports — only browser-facing datasource URLs
  (`docker/perses/datasource/`) and the host-side curls in
  `tasks/metrics.yml` carry the mapped ones.
- `pkg/responder` remains the single enforcement point for pagination and
  envelope rules on both transports; do not re-implement its rules in a
  handler.
- `kernel.Mount`'s conflict detection reads routes back from a scratch
  router before registering them; Mount implementations must stay
  registration-only for this to hold (documented in the kernel section).
- The queue's construction seeds its jobs, so any test that resolves the
  queue needs a migrated database — see `internal/registry/lifecycle_test.go`
  for the idiom.
- **Perses provisioning speaks native kinds only** (`Dashboard`, `Project`,
  `GlobalDatasource`); a `PersesDashboard` CR file is skipped silently, and
  dashboards are project-scoped — the full trap list lives in
  `.llms/architecture.md` → "observability stack (docker)".
- Documentation lookups go through MCP Context7 / DeepWiki / official docs
  first — the user requires it.

## Suggested skills

- `golang-samber-do` — before touching `internal/registry` or any service
  wiring (the whole composition root is samber/do).
- `finalize` — when the next session's work piece is done and it is time to
  close it out.

## References

- `.llms/architecture.md` — per-package rationale; read the section for the
  package before changing it.
- `docs/api-response.md` — the response contract for both transports.
- `.llms/handover-20260923_1913.md` — the audit results (clean) and the open
  work this document inherits.
- `AGENTS.md` — normative rules; this file does not repeat them.
