# Handover — 2026-09-27 00:24

Session scope: shipped two feature commits on the identity surface (current-user +
per-user group membership), relaxed TypeID contract validation to not-found
semantics, maintained the Yaak collection, and answered two user questions
(devtool 404 = by design; Yaak collection dedup).

## Repo state

Working tree is essentially clean. All session work is committed:

- `c563f4a` refactor(api): let malformed identifiers answer not_found instead of contract 400
- `45ed428` feat(identity): add current-user and per-user group membership surfaces
- `3b005c3` chore: add missing Yaak requests
- `3963d85` feat(identity): enforce password policy and harden transport responses
- `8aa4b93` fix(session): map unknown session target to not_found instead of internal

Only uncommitted changes: `api/specs/yaak.rq_64jEUFwEgw.yaml` and
`api/specs/yaak.rq_tL7krxPPnz.yaml` — Yaak app mirror rewrites that arrived after
the last commit (description refreshes on the two Type-ID devtool requests). Do not
hand-edit `api/specs/`; it is the Yaak sync mirror. Suggested commit if the user
asks: `chore(api): sync yaak collection mirror`.

Nothing was pushed (per repo rules, never push).

## What this session did (details live in the commits and memory)

1. **Current-user surface** — `UserService/GetCurrentUser` and `UpdateCurrentUser`
   RPC procedures (Authenticated rule, target = token subject, no id in request;
   UpdateCurrentUser writes profile fields only: first/last/display name + locale).
   Plus REST `PUT`/`DELETE /api/users/me/profile-picture`.
2. **Per-user group membership** — `UserGroupService/GetUserGroups` and
   `UpdateUserGroups` (Admin rule, full-replace semantics, empty list allowed,
   unknown group or account → not_found). New audit event `user_groups_updated`.
3. **TypeID validation relaxed** (user decision) — all 16
   `buf.validate.field` `string.pattern = "^user_/ugrp_…$"` constraints in
   `api/connect/*.proto` became `min_len = 1`; malformed identifiers answer
   `not_found` from the service, same as unknown ones. See commit `c563f4a`.
4. **Yaak collection** — 6 new requests created, then 6 updated (suffix
   `(unimplemented)` stripped, URLs/shapes fixed), duplicates resolved:
   one operation = one request; folder follows the owning service, not the
   upstream path. "Get user groups" lives only in User Groups
   (`rq_VGPuhUM7rm`); "Update allowed OIDC clients" lives only there too
   (`rq_q7eoYu7ykV`, pre-existing).
5. **User questions answered**: `Get user groups` in two folders was redundant
   (deduplicated per user choice); `/debug/encode-id` + `/debug/decode-id`
   answering 404 is **by design** — devtool is debug-build-only
   (`internal/transport/devtool_debug.go` vs `devtool_release.go`).

## Key gotchas learned this session (also in memory)

- **Order sensitivity, twice**: a static `/me` pattern must be declared BEFORE a
  `/{id}` pattern in BOTH the chi router (`modules/identity/user/handler.go`) AND
  the guard `RestRules` table (`internal/guard/rules.go`). Either order reversed
  makes the param pattern swallow `me` → spurious 404 (guard compares the literal
  word "me" against the caller subject).
- **audit.Record inside a transaction**: the recorder never returns errors, but a
  SQL error in the audit INSERT (e.g. `user_id` FK violation for an unknown
  account) ABORTS the surrounding tx → pgx fails at commit with "commit
  unexpectedly resulted in rollback". Any service recording an event with a
  `user_id` must verify the account exists before opening the transaction.
- **api/specs mirror lag**: the mirror is written by the running Yaak desktop
  app, not the CLI. CLI mutations (`yaak request create/update/delete --json`)
  hit the workspace immediately (verify via `yaak request show`); mirror files
  can lag indefinitely. Workspace state is the truth; never hand-edit the mirror.
- **Yaak CLI**: positional workspace id `wk_kBiMYTkhPP`; `--json` payload for
  create/update; `--yes` for delete; `yaak request schema http` for the model.
- SignIn RPC field is `identity`, not `username`.

## Environment / credentials

- Dev server: `task run` (debug build, has devtool) vs `task build` +
  `build/release/tango serve --env-file=.env.local` (release, no devtool).
  Kill stale :3080 listener before re-serving; logs in `.output/`.
- Dev sign-in: `admin` / `@dmin123` (redacted credential kept for local dev only —
  it is the repo's seeded dev account). Probe users were created and deleted;
  probe sessions signed out. A `gryffindor` group and a probe group may remain in
  the dev DB (`tango-pgsql-1`, database `postgres`) — harmless dev fixtures.
- Rate limit 60 req/60s on unauthenticated auth endpoints — wait ~65s after
  hammering.

## Open items / next steps

- None blocking. Possible future work surfaced earlier (not requested):
  refresh-token reuse/theft detection (schema change); constant-time compare in
  onetimeaccess (minor); API key zero-expiry dead-on-arrival (minor);
  impersonation feature explicitly excluded from all work per user instruction.
- If the user reports more Yaak/UI mismatches, check workspace state via
  `yaak request show <id>` first before assuming a server bug — the mirror and
  app UI can both lag or cache.

## Suggested skills

- `golang-samber-do` — before touching DI wiring (`internal/registry`,
  `modules/*/module.go`, provider rules).
- `finalize` — if the user asks to wrap up/commit; follow AGENTS.md committing
  rules (explicit paths, never push, never `git add -A`).
- `handoff` — to write the next session's handover in the same format.

## Reference artifacts

- `AGENTS.md` — repo contract (single instruction source).
- `.llms/architecture.md` — per-package rationale; "Library decision records".
- `.llms/pocket-id-api-reference.md` — upstream Pocket ID endpoint table (source
  of truth for ported shapes, alongside the code).
- `docs/api-response.md` — error-code conventions.
- Commits `45ed428` and `c563f4a` carry the full diffs of this session's feature
  and validation work; `git show` them instead of re-deriving design decisions.
