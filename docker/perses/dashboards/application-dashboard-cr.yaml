# The application's RED signal, read from the instrumentation the transport
# itself carries: otelhttp covers every API request (the http_server_* series)
# and the otelconnect interceptor covers every procedure (the rpc_server_*
# histograms). Both reach this store twice — pushed over OTLP and scraped from
# the Prometheus bridge — so a stopped collector costs history, not the live
# view.
apiVersion: perses.dev/v1alpha1
kind: Dashboard
metadata:
    name: application-monitoring
    project: tango
spec:
    display:
        name: Application Monitoring
    duration: 3h
    refreshInterval: 15s
    variables:
        - kind: ListVariable
          spec:
              display:
                  name: Instance
                  hidden: false
              allowAllValue: true
              allowMultiple: false
              plugin:
                  kind: PrometheusLabelValuesVariable
                  spec:
                      labelName: instance
                      matchers:
                          - target_info{service_name="tango"}
              name: instance
    panels:
        request-rate:
            kind: Panel
            spec:
                display:
                    name: Requests per second
                plugin:
                    kind: TimeSeriesChart
                    spec:
                        legend:
                            mode: table
                            position: bottom
                        visual:
                            areaOpacity: 0.2
                            display: line
                queries:
                    - kind: TimeSeriesQuery
                      spec:
                          plugin:
                              kind: PrometheusTimeSeriesQuery
                              spec:
                                  query: 'sum by (http_request_method) (rate(http_server_request_duration_seconds_count{service_name="tango", instance=~"$instance"}[5m]))'
        request-latency:
            kind: Panel
            spec:
                display:
                    name: Request latency (p50 / p99)
                plugin:
                    kind: TimeSeriesChart
                    spec:
                        legend:
                            mode: table
                            position: bottom
                        yAxis:
                            format:
                                unit: seconds
                queries:
                    - kind: TimeSeriesQuery
                      spec:
                          plugin:
                              kind: PrometheusTimeSeriesQuery
                              spec:
                                  query: 'histogram_quantile(0.50, sum by (le) (rate(http_server_request_duration_seconds_bucket{service_name="tango", instance=~"$instance"}[5m])))'
                    - kind: TimeSeriesQuery
                      spec:
                          plugin:
                              kind: PrometheusTimeSeriesQuery
                              spec:
                                  query: 'histogram_quantile(0.99, sum by (le) (rate(http_server_request_duration_seconds_bucket{service_name="tango", instance=~"$instance"}[5m])))'
        request-status:
            kind: Panel
            spec:
                display:
                    name: Responses by status
                plugin:
                    kind: TimeSeriesChart
                    spec:
                        legend:
                            mode: table
                            position: bottom
                        visual:
                            display: line
                queries:
                    - kind: TimeSeriesQuery
                      spec:
                          plugin:
                              kind: PrometheusTimeSeriesQuery
                              spec:
                                  query: 'sum by (http_response_status_code) (rate(http_server_request_duration_seconds_count{service_name="tango", instance=~"$instance"}[5m]))'
        active-requests:
            kind: Panel
            spec:
                display:
                    name: In-flight requests
                plugin:
                    kind: TimeSeriesChart
                    spec:
                        visual:
                            display: line
                queries:
                    - kind: TimeSeriesQuery
                      spec:
                          plugin:
                              kind: PrometheusTimeSeriesQuery
                              spec:
                                  query: 'http_server_active_requests{service_name="tango", instance=~"$instance"}'
        rpc-latency:
            kind: Panel
            spec:
                display:
                    name: Procedure latency (p99, by method)
                plugin:
                    kind: TimeSeriesChart
                    spec:
                        legend:
                            mode: table
                            position: bottom
                        yAxis:
                            format:
                                unit: milliseconds
                queries:
                    - kind: TimeSeriesQuery
                      spec:
                          plugin:
                              kind: PrometheusTimeSeriesQuery
                              spec:
                                  query: 'histogram_quantile(0.99, sum by (le, rpc_method) (rate(rpc_server_duration_milliseconds_bucket{service_name="tango", instance=~"$instance"}[5m])))'
        rpc-rate:
            kind: Panel
            spec:
                display:
                    name: Procedures per second
                plugin:
                    kind: TimeSeriesChart
                    spec:
                        legend:
                            mode: table
                            position: bottom
                        visual:
                            areaOpacity: 0.2
                            display: line
                queries:
                    - kind: TimeSeriesQuery
                      spec:
                          plugin:
                              kind: PrometheusTimeSeriesQuery
                              spec:
                                  query: 'sum by (rpc_method) (rate(rpc_server_duration_milliseconds_count{service_name="tango", instance=~"$instance"}[5m]))'
        rpc-request-size:
            kind: Panel
            spec:
                display:
                    name: Procedure request size (p99)
                plugin:
                    kind: TimeSeriesChart
                    spec:
                        yAxis:
                            format:
                                unit: bytes
                queries:
                    - kind: TimeSeriesQuery
                      spec:
                          plugin:
                              kind: PrometheusTimeSeriesQuery
                              spec:
                                  query: 'histogram_quantile(0.99, sum by (le) (rate(rpc_server_request_size_bytes_bucket{service_name="tango", instance=~"$instance"}[5m])))'
    layouts:
        - kind: Grid
          spec:
              items:
                  - x: 0
                    y: 0
                    width: 8
                    height: 8
                    content:
                        $ref: '#/spec/panels/request-rate'
                  - x: 8
                    y: 0
                    width: 8
                    height: 8
                    content:
                        $ref: '#/spec/panels/request-latency'
                  - x: 16
                    y: 0
                    width: 8
                    height: 8
                    content:
                        $ref: '#/spec/panels/request-status'
                  - x: 0
                    y: 8
                    width: 8
                    height: 8
                    content:
                        $ref: '#/spec/panels/active-requests'
                  - x: 8
                    y: 8
                    width: 8
                    height: 8
                    content:
                        $ref: '#/spec/panels/rpc-latency'
                  - x: 16
                    y: 8
                    width: 8
                    height: 8
                    content:
                        $ref: '#/spec/panels/rpc-rate'
                  - x: 0
                    y: 16
                    width: 24
                    height: 8
                    content:
                        $ref: '#/spec/panels/rpc-request-size'
