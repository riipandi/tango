# Postgres connection string (matches the pgsql service in compose.yaml).
DATABASE_URL=postgresql://postgres:securedb@localhost:5432/postgres?sslmode=disable

# Generate with: task key:generate
APP_SECRET_KEY=
AUTH_PRIVATE_KEY=
AUTH_PUBLIC_KEY=
AUTH_SECRET_KEY=
