# Handover — 2026-09-23 02:55

Working tree carries the mailer work plus **pre-existing changes from another session** (see
"Not mine" below). Repo rules: `AGENTS.md`. Package rationale: `llms/architecture.md` →
`internal/mailer`.

## What landed

`internal/mailer` is implemented on `github.com/emersion/go-smtp` v0.25.0 +
`github.com/emersion/go-sasl` (the user chose both; `go-smtp` requires `go-sasl`, so they are
adopted as a pair). `serve` resolves `*mailer.Service` before the listener opens, so a broken
template fails the run rather than the first send.

- `mailer.go` — `*Mailer`: one session per `Send`, closed by it. Implicit TLS when
  `mailer.smtp_secure`, STARTTLS when the server advertises it, plaintext (reported as a warning)
  when it does not. PLAIN then LOGIN for auth. `mailer.timeout` bounds dial, handshake, every
  command, and the body; the caller's context closes the connection through `context.AfterFunc`.
- `envelope.go` — RFC 5322 rendering: `multipart/alternative` with the text part first when both
  bodies exist, a single part otherwise; quoted-printable bodies, RFC 2047 encoded subject and
  sender name, CRLF everywhere, generated `Message-ID`. `Bcc` is an envelope recipient only.
  Address and header values carrying a line break are refused (injection).
- `error.go` — `ErrNotConfigured`, `ErrNetwork`, `ErrTimeout`, `ErrCanceled`, `ErrAuth`,
  `ErrRejected` (5xx), `ErrTemporary` (4xx), all `errors.Is`-matchable; the server's own reply text
  is never repeated in `Error()`.
- `templates.go` — `*Templates` parses the embedded `web/email/*.tmpl` as HTML/text pairs at
  construction; unknown name refused by `Render`; a name with only one rendering fails
  construction. `html/template` for HTML (escaping), `text/template` for text.
- `data.go` — one struct per template (`PasswordResetData`, …). The struct field names must match
  the `{{.Data.…}}` names the Vite build writes.
- `service.go` — `*Service` pairs the mailer and the templates; `Send` renders then submits.
- `cmd/mailersmoke.go` — `mailer:smoke`, plus `task mailer:smoke -- --to=…`.
- `internal/config` — new `mailer.timeout` (default 15s, `DefaultMailerTimeout`), listed in
  `durationKeys`, validated positive.
- `internal/registry` — `*mailer.Service` provided.
- `email/templates/api-key-expiring-soon.tsx` — `{{.Data.ApiKeyName}}` → `{{.Data.APIKeyName}}`
  so the Go struct field keeps its initialism (staticcheck ST1003).

## Decisions worth not relitigating

- **The library exports no sentinel for "this server has no STARTTLS".** `NewClientStartTLS`
  returns a plain error whose text is `smtp: server doesn't support STARTTLS`, so
  `startTLSUnsupported` matches the text and `TestStartTLSUnsupportedMatchesTheLibraryMessage`
  pins it against a real Mailpit container. A failed STARTTLS attempt closes its own connection, so
  the plaintext fallback dials again — only a TLS-less server pays for the second dial.
- **A STARTTLS session cannot be greeted explicitly.** The handshake sends its own greeting before
  the client is handed back, so it introduces itself with the client's default name; only the
  plaintext path calls `Hello(sender domain)`. Accepted, not worked around.
- **`LoginNewDeviceData.DateTime` is the one `time.Time`**, because that template formats it
  itself. Every other date is a string, because these values travel the queue as JSON.
- **The sender logo is `app.base_url` (fallback `app.assets_url`) + `/images/logoEmail.svg`.**
  `app.assets_url` defaults to `/static`, which **this transport does not mount** — the SPA is
  served at the root by `web.SetupStatic`. Using `assets_url` first produced a 404 link, so the
  public origin wins. If `/static` is ever mounted, revisit.
- **No connection pool.** An SMTP session is stateful; a pooled one would have to be verified
  before reuse. One session per send.
- **`isEmail` in `internal/config` is not reused.** It requires the bare-address form and lives in
  the config package; `internal/mailer` checks what the envelope needs (no line break, no `<>`,
  has an `@`), which is a different rule for a different purpose.

## Validation already run

`gofmt`, `go vet ./...`, `golangci-lint run -c .golangci.yml ./...` (0 issues), `go test ./...`,
`go test -tags debug ./cmd/ ./database/`, `go test -race ./internal/mailer/`, `oxfmt --check`,
`oxlint`. End to end against the compose Mailpit: `task mailer:smoke -- --to=andi@example.com`
delivered a message whose HTML and text both rendered, with the logo URL and the reset link intact.
`internal/mailer/integration_test.go` runs the same path through `testutils.StartMailpit`.

## Not mine — left untouched

These were already in the working tree when this session started, and they belong to another
worker/session; do not commit them with the mailer work:

- `vite.config.ts` (reworked plugins/root/rolldown options), `index.html` deleted, `web/index.html`
  added, `.oxfmtrc.json` / `.oxlintrc.json` (the `/index.html` ignore entry dropped).

`app.config.json` was regenerated with `config:generate --overwrite` to pick up `mailer.timeout`;
it is gitignored.

## Left alone

- `key:rotate` still prints `not yet implemented`. ConnectRPC routes and the auth middleware are
  still not mounted.
- No per-message override of the mailer timeout, and no SMTP connection reuse.
- The plaintext warning is emitted per session, so a deployment without TLS logs it on every send.
  Acceptable for a development server; a production deployment sets `mailer.smtp_secure` or has
  STARTTLS.
- `modules/**` are still scaffolds, so nothing calls `mailer.Service` yet. The first feature that
  sends mail should take it from the injector, not build one.
