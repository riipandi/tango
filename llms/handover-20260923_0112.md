# Handover — 2026-09-23 01:12

Two commits on `refine-architecture`, working tree clean, nothing pushed. Repo rules: `AGENTS.md`. Package rationale: `llms/architecture.md` → `internal/fetcher`. Do not re-copy that section into code comments or a new doc.

## What landed

1. `316c427` — `feat(fetcher): add outbound HTTP client with retry and circuit breaker`
2. `af7fa3f` — `feat(fetcher): isolate breakers per host and omit the user agent`

`internal/fetcher` is implemented. `serve` resolves `*fetcher.Client` from `internal/registry` before the listener opens. Shutdown closes host clients and idle connections on the shared `http.Client`.

Callers pass an absolute `http`/`https` URL. There is no `fetcher.base_url`. A relative URL is refused.

`fetcher.user_agent` stays on `config.Fetcher` and defaults to `config.DefaultUserAgent()` (`tango/<AppVersion> (+https://github.com/riipandi/tango)`). It is listed in `omittedKeys` in `internal/config/sample.go`, so `config:generate` and `app.config.json` do not contain it. Absence keeps the built-in token.

## Decisions worth not relitigating

- Transport is `resty.dev/v3` **v3.0.0-rc.4**. Retry defaults in Resty do not cover 5xx or non-temporary network errors; those conditions live in `internal/fetcher/error.go`. POST and PATCH are not retried. `retry_count` is capped at 5. Breaker failure threshold must exceed the retry count. Reset timeout must be at least one attempt timeout.
- Resty's breaker is per client and `Allow()` does not receive the request, so each host (`scheme://host`) gets its own `*resty.Client` and breaker. They share one `http.Client` / transport.
- `fetcher.max_body_bytes` defaults to 16 MiB and `Validate` refuses anything above 32 MiB.
- Debug body logging and the cookie jar stay off. Redirects strip `Authorization` and `Cookie`.
- Outbound calls start a client span and inject W3C `traceparent` plus baggage. `observer.SetGlobals` installs that propagator; the OTel global default injects nothing. `internal/fetcher/trace.go` falls back to the same pair when the global propagator has no fields.
- Error text is method, scheme, host, and path. Query, userinfo, headers, and bodies are not logged. Match with `errors.Is`: `ErrNetwork`, `ErrTimeout`, `ErrCanceled`, `ErrRetryExhausted`, `ErrCircuitOpen`, `ErrStatus`.

## Validation already run

On `af7fa3f`: `gofmt`, `golangci-lint run -c .golangci.yml` for `./internal/fetcher/...`, `./internal/config/...`, `./internal/observer/...`, `./cmd/...` (0 issues), and `go test` for those packages. An earlier full `go test ./...` and `go test -tags debug ./cmd/ ./database/` passed on the first fetcher commit; they were not re-run after `af7fa3f`.

`app.config.json` matches `config:generate` (the starter file, not a user-owned dotenv).

## Left alone

- Modules under `modules/**` are still scaffolds. ConnectRPC routes and auth middleware are still not mounted.
- Resty v3 is still a release candidate. Revisit the pin when a stable v3 exists; do not bump it casually. After any `go get`, re-check the OTel `otel/log` pins at v0.19.0 (`llms/architecture.md`, Library decision records).
- No per-upstream override of timeout or retry. Every host uses `config.Fetcher`.
- The host map grows for the life of the process. No eviction.

## Suggested skills

- **golang-samber-do** — before changing `internal/registry` or how `*fetcher.Client` is provided.
- **go-http-safeguards** — when the next session builds ConnectRPC routes or middleware in `internal/transport`.
- **explore-codebase** — when wiring the first feature module that calls `fetcher.Do`.
