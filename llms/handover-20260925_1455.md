# Handover — 2026-09-25 14:55

Branch `refactor-observability-stack` era of `tango`. Continues from
`.llms/handover-20260924_0100.md` (still accurate for everything before this
session; do not re-read it here — this file supersedes its "next steps").

## What this session delivered (oldest → newest)

All items below are committed except the last one. The user committed between
turns; the last three commits are:

- `feat(observability): make metrics pull-first and make OTLP metric push opt-in`
- `chore(observability): recreate more robust Perses dashboard`
- `feat(observability): add SLO dashboard, edge access logs, and fix empty-variable dashboards`

1. **Metrics ingestion is pull-first** — the Prometheus bridge at
   `otel.metrics.prometheus_path` is the primary route; the OTLP metric push is
   opt-in via `otel.metrics.push` (default false, validated to require
   `otel.metrics.enable`). Rationale and config contract live in
   `.llms/architecture.md` (internal/observer section) — do not restate them here.

2. **Dashboards rebuilt** — `docker/perses/dashboards/` now holds 10 dashboards,
   filenames without the `-cr` suffix: `system-overview` (integrated one-pane),
   `application-monitoring` (metrics only), `logs-traces` (LogsTable + TraceTable
   side by side, shared `service` variable — the user's explicit choice over
   merging traces into app monitoring), `go-runtime-metrics`, `nginx-edge`,
   `postgres-database`, `valkey-cache`, `pipeline-observability`,
   `slo-error-budget` (targets are defaults: 99.9% availability, 99% under
   300ms, 30d window; burn-rate panels are the future vmalert surface),
   `application-internals` (see item 4, uncommitted). RPC panels use
   `rpc_server_duration_milliseconds` (not `_seconds` — that name does not
   exist) and convert to seconds for display.

3. **Edge access logs pipeline** — nginx writes one JSON line per request
   (`log_format json_edge` in the compose `nginx_config` template) to the
   shared `../.storage/nginx_logs` bind mount; Vector reads it (file source →
   remap → plain `http` sink) into VictoriaLogs
   `insert/jsonline?_stream_fields=service.name`. The `nginx-edge` dashboard
   gained three LogsQL panels (status split, 5xx/s, p50/p95 request time).
   Traps recorded in `.llms/architecture.md` observability section: the
   `opentelemetry` sink with `codec: otlp` only accepts raw OTLP envelopes, and
   LogsQL braces match only `_stream` fields.

4. **Subsystem instrumentation — UNCOMMITTED (the main thing to land next)**.
   Each package defines its own `metrics.go` building OTel instruments in the
   owner's constructor from `otel.Meter(<package scope>)` — never a
   process-level `sync.Once` (instruments built before `SetGlobals` stay no-op
   forever and cannot be unit-tested). The per-package contract, metric names,
   and outcome labels are documented in `.llms/architecture.md` (queue,
   scheduler, storage, internal/fetcher, internal/transport sections) — read
   those before changing anything. Signature change to know about:
   `middleware.RateLimit(surface string, limiter, refuse, excluded...)` and
   `scheduler.claim` now returns `(outcome string, error)`.
   `docker/perses/dashboards/application-internals.yaml` (16 panels) reads the
   new series.

## Uncommitted working tree (verify before staging — do not stage unprompted)

- **This session's work** (validated: `go test ./...` green, `task lint`
  0 issues, `task check` green, live smoke passed):
  `internal/{queue,scheduler,storage,fetcher}/metrics.go` (+ the two
  `metrics_test.go`), edits in `internal/queue/{client,dispatcher}.go`,
  `internal/scheduler/{scheduler,scheduler_test}.go`,
  `internal/storage/{manager,watcher}.go`, `internal/fetcher/fetcher.go`,
  `internal/transport/middleware/{ratelimit,ratelimit_metrics}.go` (+ test),
  `internal/transport/router_http.go`,
  `docker/perses/dashboards/application-internals.yaml`.
  Recommended commit: `feat(observability): instrument the queue, scheduler,
  storage, fetcher, and rate limiter`.
- **The user's own edits, mixed into the same tree**: `docker/compose-metrics.yaml`
  was deleted and replaced by `docker/compose-dev.yaml` + `docker/compose-sre.yaml`
  (now included from `docker/compose.yaml`), plus edits to `AGENTS.md`,
  `README.md`, `Taskfile.yml`, `.llms/database-reference.sql`,
  `.llms/handover-20260922_0310.md`, `tasks/metrics.yml`. These are the user's;
  confirm before touching or staging them. If compose services were renamed,
  check that `tasks/metrics.yml` targets and the `tango`/`vector`/`nginx` scrape
  jobs still line up with the new file layout.

## Session-specific traps (not already in AGENTS.md / architecture.md)

- **Perses provisioning creates, never overwrites**: to apply a changed
  dashboard file, `DELETE /api/v1/projects/tango/dashboards/<name>` (204) then
  restart the perses container. The whole `docker/perses/` dir is mounted at
  `/etc/perses/provisioning`; file names are free-form.
- **Perses `ListVariable` without `defaultValue` interpolates empty**
  (`instance=~""` → zero series → "dashboard shows no data"). Every
  instance/datname variable carries `defaultValue` + `customAllValue: ".*"` —
  keep that pattern for new dashboards.
- **Temp smoke config durations must be strings**: `"cleanup_interval": "5s"`.
  A bare `5` parses as 5 **nanoseconds** and turns the recurring-job loop into
  a hot loop (observed: 3,240 tasks in minutes).
- **Docker Desktop `host-gateway` can resolve to a bogus IP** (seen:
  `0.250.250.254`, VPN-related). Symptom: VM's `tango` scrape target fails from
  containers while the app is healthy. Fix is a Docker Desktop restart, not a
  code change; verify with `/api/v1/targets`.
- **Live smoke recipe that worked**: copy `app.config.json` to a temp dir with
  `queue.cleanup_interval: "5s"`, run
  `build/debug/tango --env-file=<tmp env with OTEL_METRICS_ENABLE=true …> serve`
  from that dir, hit `/.well-known/jwks.json` (rate-limit counter), drop a file
  into `<local_path>/staging/` (watcher → upload task → sync → storage
  counters), scrape `/metrics` for the `tango_*` series. Scheduler cron ticks
  and fetcher calls have no live driver in the default app (no cron jobs
  registered, no outbound callers) — they are pinned by unit tests
  (`sdkmetric` ManualReader; note `Int64Counter` → `Sum[int64]` and
  `Int64Histogram` → `Histogram[int64]` in `metricdata`).

## Validation

`go test ./...`, `task lint` (0 issues), `task check`, and for dashboards: the
query validator pattern used in this session (substitute `$instance`/`$service`,
check every `PrometheusTimeSeriesQuery` against `:33802/api/v1/query` and every
`VictoriaLogsTimeSeriesQuery` against `:33801/select/logsql/stats_query_range`).

## Next steps (in the order the user is likely to want)

1. Land the instrumentation commit (see above) — everything is validated.
2. Review/commit the user's compose restructure (`compose-dev`/`compose-sre`)
   with them; make sure `tasks/metrics.yml` still matches.
3. vmalert + Alertmanager on top of the SLO dashboard's burn-rate panels (the
   user already chose Perses-over-Grafana with vmalert as the alerting answer —
   see `.llms/architecture.md` "Library decision records").
4. Candidate code fix flagged this session: otelconnect's `rpc_server_*`
   histograms carry `net_peer_name`/`net_peer_port` (per-connection series — a
   cardinality leak). Dropping those attributes in instrumentation would shrink
   the store; dashboards must not depend on them.
5. Storage/fetcher metrics have no live drivers yet beyond the local smoke; if
   an S3 deployment or the first outbound-calling feature lands, add a panel
   check for the `driver="s3"` / per-host breakdowns.

## Suggested skills

- `golang-samber-do` — before any wiring change in `internal/registry` (the
  instrumentation threads no new services, but the next step might).
- `finalize` — when closing out the instrumentation commit (the session-ending
  checklist it runs matches what this session validated).
- `handoff` — to refresh this document once the uncommitted work is landed.
