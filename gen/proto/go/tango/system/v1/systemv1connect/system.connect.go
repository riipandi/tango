// Canonical internal RPC contract for transport-owned system surfaces.
// The public health endpoints stay REST; HealthService exists only as
// the Connect transport smoke target.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: system.proto

package systemv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/riipandi/tango/gen/proto/go/tango/system/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// HealthServiceName is the fully-qualified name of the HealthService service.
	HealthServiceName = "tango.system.v1.HealthService"
	// VersionServiceName is the fully-qualified name of the VersionService service.
	VersionServiceName = "tango.system.v1.VersionService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// HealthServiceCheckProcedure is the fully-qualified name of the HealthService's Check RPC.
	HealthServiceCheckProcedure = "/tango.system.v1.HealthService/Check"
	// VersionServiceCurrentProcedure is the fully-qualified name of the VersionService's Current RPC.
	VersionServiceCurrentProcedure = "/tango.system.v1.VersionService/Current"
	// VersionServiceLatestProcedure is the fully-qualified name of the VersionService's Latest RPC.
	VersionServiceLatestProcedure = "/tango.system.v1.VersionService/Latest"
)

// HealthServiceClient is a client for the tango.system.v1.HealthService service.
type HealthServiceClient interface {
	Check(context.Context, *connect.Request[v1.CheckRequest]) (*connect.Response[v1.CheckResponse], error)
}

// NewHealthServiceClient constructs a client for the tango.system.v1.HealthService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewHealthServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) HealthServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	healthServiceMethods := v1.File_system_proto.Services().ByName("HealthService").Methods()
	return &healthServiceClient{
		check: connect.NewClient[v1.CheckRequest, v1.CheckResponse](
			httpClient,
			baseURL+HealthServiceCheckProcedure,
			connect.WithSchema(healthServiceMethods.ByName("Check")),
			connect.WithClientOptions(opts...),
		),
	}
}

// healthServiceClient implements HealthServiceClient.
type healthServiceClient struct {
	check *connect.Client[v1.CheckRequest, v1.CheckResponse]
}

// Check calls tango.system.v1.HealthService.Check.
func (c *healthServiceClient) Check(ctx context.Context, req *connect.Request[v1.CheckRequest]) (*connect.Response[v1.CheckResponse], error) {
	return c.check.CallUnary(ctx, req)
}

// HealthServiceHandler is an implementation of the tango.system.v1.HealthService service.
type HealthServiceHandler interface {
	Check(context.Context, *connect.Request[v1.CheckRequest]) (*connect.Response[v1.CheckResponse], error)
}

// NewHealthServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewHealthServiceHandler(svc HealthServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	healthServiceMethods := v1.File_system_proto.Services().ByName("HealthService").Methods()
	healthServiceCheckHandler := connect.NewUnaryHandler(
		HealthServiceCheckProcedure,
		svc.Check,
		connect.WithSchema(healthServiceMethods.ByName("Check")),
		connect.WithHandlerOptions(opts...),
	)
	return "/tango.system.v1.HealthService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case HealthServiceCheckProcedure:
			healthServiceCheckHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedHealthServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedHealthServiceHandler struct{}

func (UnimplementedHealthServiceHandler) Check(context.Context, *connect.Request[v1.CheckRequest]) (*connect.Response[v1.CheckResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tango.system.v1.HealthService.Check is not implemented"))
}

// VersionServiceClient is a client for the tango.system.v1.VersionService service.
type VersionServiceClient interface {
	Current(context.Context, *connect.Request[v1.CurrentRequest]) (*connect.Response[v1.CurrentResponse], error)
	Latest(context.Context, *connect.Request[v1.LatestRequest]) (*connect.Response[v1.LatestResponse], error)
}

// NewVersionServiceClient constructs a client for the tango.system.v1.VersionService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewVersionServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) VersionServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	versionServiceMethods := v1.File_system_proto.Services().ByName("VersionService").Methods()
	return &versionServiceClient{
		current: connect.NewClient[v1.CurrentRequest, v1.CurrentResponse](
			httpClient,
			baseURL+VersionServiceCurrentProcedure,
			connect.WithSchema(versionServiceMethods.ByName("Current")),
			connect.WithClientOptions(opts...),
		),
		latest: connect.NewClient[v1.LatestRequest, v1.LatestResponse](
			httpClient,
			baseURL+VersionServiceLatestProcedure,
			connect.WithSchema(versionServiceMethods.ByName("Latest")),
			connect.WithClientOptions(opts...),
		),
	}
}

// versionServiceClient implements VersionServiceClient.
type versionServiceClient struct {
	current *connect.Client[v1.CurrentRequest, v1.CurrentResponse]
	latest  *connect.Client[v1.LatestRequest, v1.LatestResponse]
}

// Current calls tango.system.v1.VersionService.Current.
func (c *versionServiceClient) Current(ctx context.Context, req *connect.Request[v1.CurrentRequest]) (*connect.Response[v1.CurrentResponse], error) {
	return c.current.CallUnary(ctx, req)
}

// Latest calls tango.system.v1.VersionService.Latest.
func (c *versionServiceClient) Latest(ctx context.Context, req *connect.Request[v1.LatestRequest]) (*connect.Response[v1.LatestResponse], error) {
	return c.latest.CallUnary(ctx, req)
}

// VersionServiceHandler is an implementation of the tango.system.v1.VersionService service.
type VersionServiceHandler interface {
	Current(context.Context, *connect.Request[v1.CurrentRequest]) (*connect.Response[v1.CurrentResponse], error)
	Latest(context.Context, *connect.Request[v1.LatestRequest]) (*connect.Response[v1.LatestResponse], error)
}

// NewVersionServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewVersionServiceHandler(svc VersionServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	versionServiceMethods := v1.File_system_proto.Services().ByName("VersionService").Methods()
	versionServiceCurrentHandler := connect.NewUnaryHandler(
		VersionServiceCurrentProcedure,
		svc.Current,
		connect.WithSchema(versionServiceMethods.ByName("Current")),
		connect.WithHandlerOptions(opts...),
	)
	versionServiceLatestHandler := connect.NewUnaryHandler(
		VersionServiceLatestProcedure,
		svc.Latest,
		connect.WithSchema(versionServiceMethods.ByName("Latest")),
		connect.WithHandlerOptions(opts...),
	)
	return "/tango.system.v1.VersionService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case VersionServiceCurrentProcedure:
			versionServiceCurrentHandler.ServeHTTP(w, r)
		case VersionServiceLatestProcedure:
			versionServiceLatestHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedVersionServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedVersionServiceHandler struct{}

func (UnimplementedVersionServiceHandler) Current(context.Context, *connect.Request[v1.CurrentRequest]) (*connect.Response[v1.CurrentResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tango.system.v1.VersionService.Current is not implemented"))
}

func (UnimplementedVersionServiceHandler) Latest(context.Context, *connect.Request[v1.LatestRequest]) (*connect.Response[v1.LatestResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tango.system.v1.VersionService.Latest is not implemented"))
}
