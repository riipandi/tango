package oidc

// TODO: authorization codes, access/refresh tokens with rotation, ID
// token signing keys (JWKS), introspection and revocation. Signing
// keys reach the discovery feature via a consumer-side adapter in
// the registry.
